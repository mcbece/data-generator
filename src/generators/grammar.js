import { each, objectGet } from "../lib/util.js"
import logger from "../lib/logger.js"
import { LIST_CONFIGURATIONS } from "../data/data.js"

const defTmpl = {
    "input": {
        "text": "{This: name} "
    }
}

export default function(data, dest, _env) {
    
}
